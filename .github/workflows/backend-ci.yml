name: Backend CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Build and test
        run: |
          docker-compose -f infrastructure/docker-compose.yml build
          docker-compose -f infrastructure/docker-compose.yml run backend pytest
